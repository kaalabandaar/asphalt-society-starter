import { Nav } from '@/components/Nav';import { featuredProducts } from '@/lib/data';
export default async function Product({params}:{params:Promise<{id:string}>}){const {id}=await params;const p=featuredProducts.find(x=>x.id===id)||featuredProducts[0];return <main><Nav/><section className="grid gap-10 px-6 pt-32 md:grid-cols-2 md:px-16"><div className="h-[600px] rounded-3xl bg-gradient-to-br from-white/15 to-black"/><div><p className="text-asphalt-red">{p.category}</p><h1 className="font-display text-7xl">{p.name}</h1><p className="mt-4 text-3xl font-bold">${p.price}</p><p className="mt-6 text-asphalt-smoke">Premium fitment-focused part with cinematic build compatibility, install notes, reviews, and related mods.</p><button className="mt-8 rounded-full bg-asphalt-red px-8 py-4 font-bold">Add To Cart</button></div></section></main>}
