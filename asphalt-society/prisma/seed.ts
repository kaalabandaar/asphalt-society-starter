import { PrismaClient } from '@prisma/client';const prisma=new PrismaClient();async function main(){await prisma.product.upsert({where:{slug:'midnight-carbon-front-lip'},update:{},create:{name:'Midnight Carbon Front Lip',slug:'midnight-carbon-front-lip',description:'Aggressive carbon-style front lip.',price:39900,category:'Aero Kits',inventory:12,images:[]}})}main().finally(()=>prisma.$disconnect());
