'use client';
import { useConfig } from '@/lib/configurator-store';
export function ConfiguratorPanel(){const s=useConfig(); const colors=['#050505','#ffffff','#ff2b2b','#1b2333','#6b7280','#16331f'];return <aside className="glass rounded-3xl p-5"><h2 className="font-display text-5xl">CONFIGURE</h2><label className="mt-6 block text-sm uppercase tracking-[.25em] text-asphalt-smoke">Make / Model / Year</label><select className="mt-2 w-full rounded-xl bg-black p-3"><option>2019 Audi A7</option><option>Nissan Skyline R34</option><option>Toyota Supra MK4</option><option>Dodge Charger Hellcat</option></select><div className="mt-6"><p className="text-sm uppercase tracking-[.25em] text-asphalt-smoke">Paint / Wrap</p><div className="mt-3 flex gap-2">{colors.map(c=><button key={c} onClick={()=>s.set({paint:c})} className="h-10 w-10 rounded-full border border-white/30" style={{background:c}} />)}</div></div><label className="mt-6 block">Suspension lowering<input type="range" min="0" max="0.35" step="0.01" value={s.lowering} onChange={e=>s.set({lowering:Number(e.target.value)})} className="w-full"/></label><label className="mt-4 block">Window tint<input type="range" min="0" max="0.55" step="0.01" value={s.tint} onChange={e=>s.set({tint:Number(e.target.value)})} className="w-full"/></label><div className="mt-5 grid grid-cols-2 gap-3"><button onClick={()=>s.set({spoiler:!s.spoiler})} className="rounded-xl bg-white/10 p-3">Toggle Spoiler</button><button onClick={()=>s.set({carbon:!s.carbon})} className="rounded-xl bg-white/10 p-3">Carbon Parts</button></div><div className="mt-5 grid grid-cols-2 gap-3"><button className="rounded-full bg-asphalt-red p-3 font-bold">Save Build</button><button className="rounded-full bg-white p-3 font-bold text-black">Add To Cart</button></div></aside>}
